# GoLand custom VM options.
# These are merged into each GoLand version's goland.vmoptions by install-paths.
# Lines here override matching flags in the existing file.
# Toolbox-managed lines (-Dide.managed.by.toolbox, -Dtoolbox.*) are preserved.

# Max heap size. Default: 2048m
-Xmx1536m
