#!/usr/bin/env bash

set -e # exit on failure
set -u # fail if vars are not defined

if !command -v sentry-cli --version &>/dev/null; then
	curl -sL https://sentry.io/get-cli/ | SENTRY_CLI_VERSION="2.26.0" sh
fi

sentry-cli "$@"
