#!/usr/bin/env bash

# tails the specified files using tailspin

set -euo pipefail

amt="-1000"
if [ $# -gt 0 ]; then
    echo "hi: $1"
    if [[ "$1" =~ [+-][0-9]+ ]]; then
        amt="$1"
        shift
    fi
fi

tail -F -n "$amt" "$@" | tspin

